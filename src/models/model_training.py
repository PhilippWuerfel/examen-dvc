"""
Using the parameters found through GridSearch, we will train the model and save the trained model in the models directory.
"""
# import joblib
# # dump model
# joblib.dump(search, f"models/{model_name}.pk1")